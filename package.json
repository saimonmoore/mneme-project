{
  "name": "mneme",
  "private": true,
  "version": "0.0.0",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "license": "MIT",
  "scripts": {
    "turbo": "turbo",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier-config": "*"
  },
  "resolutions": {
    "typescript": "5.4.5"
  }
}
