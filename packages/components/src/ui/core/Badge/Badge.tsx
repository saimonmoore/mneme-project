import { Badge as GluestackBadge } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const Badge = styled(GluestackBadge, {
  // bg: "$primary600",
  // pt: "$6",
});
