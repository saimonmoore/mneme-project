import { LinkText as GluestackLinkText } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const LinkText = styled(GluestackLinkText, {
  // bg: "$primary600",
  // pt: "$6",
});