import { SectionList as GluestackSectionList } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const SectionList = styled(GluestackSectionList, {
  // bg: "$primary600",
  // pt: "$6",
});
