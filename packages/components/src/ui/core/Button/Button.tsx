import { Button as GluestackButton } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const Button = styled(GluestackButton, {
  // bg: "$primary600",
  // pt: "$6",
});
