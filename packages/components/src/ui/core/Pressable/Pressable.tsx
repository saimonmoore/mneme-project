import { Pressable as GluestackPressable } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const Pressable = styled(GluestackPressable, {
  // bg: "$primary600",
  // pt: "$6",
});
