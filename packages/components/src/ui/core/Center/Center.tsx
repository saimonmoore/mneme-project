import { Center as GluestackCenter } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const Center = styled(GluestackCenter, {
  // bg: "$primary600",
  // pt: "$6",
});
