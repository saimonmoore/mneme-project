import { VirtualizedList as GluestackVirtualizedList } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const VirtualizedList = styled(GluestackVirtualizedList, {
  // bg: "$primary600",
  // pt: "$6",
});
