import { BadgeText as GluestackBadgeText } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const BadgeText = styled(GluestackBadgeText, {
  // bg: "$primary600",
  // pt: "$6",
});
