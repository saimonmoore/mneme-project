import { ButtonText as GluestackButtonText } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const ButtonText = styled(GluestackButtonText, {
  // bg: "$primary600",
  // pt: "$6",
});