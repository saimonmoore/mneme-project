import { BadgeIcon as GluestackBadgeIcon } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const BadgeIcon = styled(GluestackBadgeIcon, {
  // bg: "$primary600",
  // pt: "$6",
});
