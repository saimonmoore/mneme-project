import { HStack as GluestackHStack } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const HStack = styled(GluestackHStack, {
  // bg: "$primary600",
  // pt: "$6",
});
