import { VStack as GluestackVStack } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const VStack = styled(GluestackVStack, {
  // bg: "$primary600",
  // pt: "$6",
});
