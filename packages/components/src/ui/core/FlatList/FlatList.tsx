import { FlatList as GluestackFlatList } from "@gluestack-ui/themed";
import { styled } from "@gluestack-style/react";

export const FlatList = styled(GluestackFlatList, {
  // bg: "$primary600",
  // pt: "$6",
});
